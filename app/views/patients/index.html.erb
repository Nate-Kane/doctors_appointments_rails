<h1>Patients</h1>
      <%= link_to 'Add Patient', new_patient_path %>
      <hr />
      <ul>
        <% @patients.each do |patient| %>
          <li>
            <%= link_to patient.full_name, patient %> 
  <!--  define full_name in the Patients model, to be able to call it here -->
          </li>
      <% end %>
      </ul>